/**
 * @deprecated
 * TaskService has been moved to `features/tasks/services/task.service.ts`.
 * This file is a compatibility shim — update imports to point to the new path.
 */
export { TaskService } from '../../../features/tasks/services/task.service';
export type { TaskData, UpdateTaskRequest } from '../../../features/tasks/models/task.models';
